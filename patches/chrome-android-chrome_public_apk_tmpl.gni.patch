diff --git a/chrome/android/chrome_public_apk_tmpl.gni b/chrome/android/chrome_public_apk_tmpl.gni
index 6bd4fc5504337705757f4f1e8683e5bcb8d4043a..8dd4d3a82087251ae4a8ccda09177d560451b5a1 100644
--- a/chrome/android/chrome_public_apk_tmpl.gni
+++ b/chrome/android/chrome_public_apk_tmpl.gni
@@ -291,6 +291,7 @@ template("chrome_common_apk_or_module_tmpl") {
       "*ic_file_download_white*",  # Bottom edge seems misaligned.
       "*ic_lock.*",  # Bottom edge seems misaligned.
     ]
+    resource_exclusion_exceptions += brave_resources_exclusion_exceptions
 
     # Most of these, with the exception of resource_exclusion_exceptions,
     # are currently duplicated in system_webview_apk_tmpl.gni.
@@ -315,6 +316,7 @@ template("chrome_common_apk_or_module_tmpl") {
 
       proguard_enabled = true
       proguard_configs = [ "//chrome/android/proguard/main.flags" ]
+      proguard_configs += [ "//brave/android/java/proguard.flags" ]
       if (_is_test) {
         proguard_configs += [ "//chrome/android/proguard/apk_for_test.flags" ]
       }
@@ -666,6 +668,10 @@ template("chrome_common_apk_or_module_tmpl") {
                                  "version_code",
                                  "version_name",
                                ])
+    _wireguard_target = "//brave/third_party/android_deps:com_wireguard_android_J__unpack_aar" deps += [ _wireguard_target] _libwireguard_dir = get_label_info("//brave/third_party/android_deps:com_wireguard_android($default_toolchain)","target_out_dir") + "/com_wireguard_android_java/jni" loadable_modules +=[ "$_libwireguard_dir/$android_app_abi/libwg-go.so" ]
+    # Browther: Sawtunaa Voie B (pièce #6) — pattern wireguard pour packager
+    # libonnxruntime.so + libonnxruntime4j_jni.so dans chrome_public_apk.
+    _ort_target = "//brave/third_party/browther_android_deps:onnxruntime_android_J__unpack_aar" deps += [ _ort_target ] _libort_dir = get_label_info("//brave/third_party/browther_android_deps:onnxruntime_android($default_toolchain)","target_out_dir") + "/onnxruntime_android_java/jni" loadable_modules += [ "$_libort_dir/$android_app_abi/libonnxruntime.so", "$_libort_dir/$android_app_abi/libonnxruntime4j_jni.so" ]
   }
 }
 
